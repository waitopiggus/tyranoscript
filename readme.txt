This is a drop-in fork for western /g/entlemen:
- Linux support
- Google autotranslator (which is an abomination, but see below)
- .ks files are scraped for lines and. The initial machine JP=EN translations are stored into a flat file to be fixed by hand, with live reload.
